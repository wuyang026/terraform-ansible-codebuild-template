- name: SSM base test
  hosts: dbservers
  gather_facts: no

  tasks:
    - name: test connection
      raw: echo "SSM connected"

    - name: python check
      raw: python3 --version || echo "no python3"