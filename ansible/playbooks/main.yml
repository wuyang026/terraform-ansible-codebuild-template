---
- name: Common services for all servers
  hosts: dbservers
  become: yes
  roles:
    - common

# 個別サービスは roles/db などに分離して追加できます
# 例:
# - name: DB専用サービス
#   hosts: dbservers
#   become: yes
#   roles:
#     - db